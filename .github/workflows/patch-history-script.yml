name: Patch history script
on:
  push:
    paths:
      - 'history.js'
      - '.github/workflows/patch-history-script.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add history.js to index
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text()
          tag='<script src="history.js"></script>'
          if tag not in s:
              anchor='<script src="account.js"></script>'
              if anchor not in s:
                  raise SystemExit('account.js anchor not found')
              s=s.replace(anchor, anchor+'\n'+tag)
              p.write_text(s)
          PY
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Load complete History interface"
          git push
